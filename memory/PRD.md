# Axiom (Enterprise AI Accounting SaaS) — PRD

## Original problem statement
Build an enterprise-level AI accounting SaaS software. Features include manual/auto transaction management, AI categorization, Plaid/Veryfi integration, white-labeling, 3-tier branding cascade, Partner roles with scoped data, and QBO bi-directional sync. Complete 1:1 report parity (Accrual & Cash basis) between Axiom and QuickBooks Online, fixing mapping discrepancies, missing transaction types, and deploying a multi-company backfill to correct historical data drift.

## Personas
- **Superadmin** — platform owner (michael@bigsaas.ai, admin@axiom.ai)
- **Pro / Accountant** — firm CPA managing many client books (pro@axiom.ai)
- **Partner** — reseller with white-labeled sub-brand (partner@axiom.ai / AxiomPartners)
- **Client** — small-business owner viewing their own books (client@axiom.ai, client2@axiom.ai)
- **UK Demo user** — non-US persona (demo-uk@smartbooks.ai)

## Key Architectural Concepts
- **GL-as-source-of-truth**: For QBO-connected companies, `qbo_gl_lines` (raw QBO GL, dual-basis) drives every summary report for 1:1 parity. Legacy entities (`transactions/invoices/payments/bills`) only power drill-downs.
- **3-tier branding cascade**: Enterprise → Partner → Company overrides.
- **Preview → Prod hydration**: Real prod companies are cloned into preview with `id` suffix `-preview-clone` (Emeral Coast, BM QBO 2, Nicole Pettyjohn).


## What's New (2026-02-28)
- **Mobile UX Phase 1 (2026-02-28)** — Fully separate mobile shell that only kicks in ≤767px, leaving desktop 100% untouched. New pieces:
  - `lib/useIsMobile.js` — single-source viewport hook (`window.innerWidth <= 767`)
  - `components/MobileShell.jsx` — `<MobileTopBar>` (hamburger + company switcher + bell + profile, sticky top, safe-area padding), `<MobileDrawer>` (slide-in from left, contains full Sidebar, scrim-close, body-scroll-lock, auto-closes on route change), `<MobileBottomNav>` (4 tabs: Home / CRM / Accounting / Chat — tabs filtered against `enabled_products` so users only see modules they have access to; Chat emits `ai-open` event to re-open AiPanel)
  - `components/Layout.jsx` — split-render: on mobile boots into a lean chrome (top bar + drawer + bottom nav + safe-area-aware `<main>` padding), skips the desktop sidebar + product rail + AI panel by default (chat tab re-opens the AiPanel on demand)
  - Existing `<CompanySwitcher>` / `<ProfileMenu>` promoted to named exports so MobileShell can reuse them without duplication
- **Transactions — Swipe cards on mobile (2026-02-28)** — New `components/MobileTxnCards.jsx`. On mobile the huge desktop table is replaced with a swipe-friendly card list (date + payee + amount + category + AI-suggested chip). Gestures: swipe right = accept AI proposal / apply suggested category, swipe left = expand inline `AccountPicker` for re-categorization. Uses the SAME `updateCategory` mutation as the desktop table so both flows converge on one code path. Desktop table untouched.
- **PrintChecks — Sticky action bar on mobile (2026-02-28)** — On mobile the "$XXX total · Preview / Print" bar becomes a fixed bottom overlay above the nav rail. Print button abbreviates to "Print" instead of "Print & Commit" to save mobile width. Buttons now have `min-h-[44px]` on all viewports.
- **Reports — Horizontal-scroll shell on mobile (2026-02-28)** — Wrapping `ReportView` in an `overflow-x-auto` on `max-md:` so wide 12-column grids (BS/P&L/TB) become swipeable rather than crushed.
- **Global — Safe-area + slide-in + tap-target CSS (2026-02-28)** — `index.css`: safe-area utility classes (`.pt-safe`, `.pb-safe`), slide-in keyframe for the drawer, and a mobile-only `button { min-height: 40px }` global that grows tap targets on phones without disturbing desktop density.
- **PWA / Mobile App — Phase 1 (2026-02-28)** — SmartBooks / CypherPro is now installable as a home-screen app on any phone (Chrome/Android natively, iOS 16.4+ via Add-to-Home-Screen). Backend: `push.py` + `routes/pwa.py`; VAPID keys in `.env`; `pywebpush` dependency. Frontend: `public/service-worker.js`, `public/offline.html`, `lib/pwa.js`, `components/InstallPrompt.jsx` (Card + Toast + `<InstallRibbon />` on `/home`), `pages/NotificationSettings.jsx` at `/settings/notifications`. `notify()` fan-outs to `send_web_push()`. Per-tenant branding — `/api/manifest.json` reads Host and returns the tenant's brand (name, colors, icon). Tests: `tests/test_pwa.py` (4 cases).
- **Print Checks — MVP Phase 1 (2026-02-28)** — New `/accounting/checks` page under Accounting → **Print Checks**. **Five layouts** now via server-driven registry (`LAYOUTS` in `routes/checks.py`) — Voucher-on-top, Voucher-in-middle, Voucher-on-bottom, Standard 3-up business checks (no stub), Wallet 3-up personal checks. Frontend renders a live SVG example of the selected layout in the space next to the dropdown, plus compatible-stock hints (VersaCheck / Deluxe / ADP part numbers) for each. Pick bank → next check # auto-fills from `accounts.next_check_number` → select unpaid bills (grouped by payee) or add ad-hoc checks → **Preview PDF** (no ledger writes) or **Print & Commit** (creates one `payments` doc per linked bill, marks bills paid via existing `create_payment` flow, saves a `checks` history row, bumps next check #). MICR clear-band left blank — assumes user is on pre-printed magnetic-toner stock. Manual sign only for MVP. Void action marks a check unusable for reuse (audit). Backend: `routes/checks.py` + `tests/test_check_printing.py` (5 tests). Adding a new layout is one dict entry in `LAYOUTS` + optional custom renderer.
- **CRM-gated User Settings tab (2026-02-28)** — In `CompanySettings.jsx` the "User Settings" tab (Nav Style, Meeting Links, AI Note Takers) is now filtered out of the tab strip when `me.enabled_products` does not include `crm`. Persisted tab preference falls back to the first available tab if it was previously "user". Verified with `client2@axiom.ai` (accounting-only → tab hidden) and `client@axiom.ai` (crm+accounting → tab visible).
- **Pricing catalog update (2026-02-28)** — Essentials $75 → **$79**/mo, Plus $97 → **$95**/mo in `enterprises.PRICE_CATALOG` (flows through `/api/pro/billing/context` into the New Client dialog price hint).
- **Voice Actions — Round 7.5: Conversational Review Closer (2026-02-28)** — Once the plan resolves with no pending clarifications AND at least one action, the AiPanel posts a "Got 1 note, 2 emails ready. Say **'looks good'** to save, or tell me what to change" card with a green hint pill. The next chat turn is routed as: commit (`looks good` / `confirm` / `ship it` / `perfect` / etc.), cancel (`cancel` / `scratch that` / `nevermind`), or refinement (anything else → re-plans via the clarification bus). New events: `axiom:voice-review-ready`, `axiom:voice-review-confirm`, `axiom:voice-review-cancel`.
- **Voice Actions — Round 7.4: Clarifications in Chat (2026-02-28)** — Ambiguity questions ("You said 'Larry OR John' — which one?") no longer render inside the middle popup. They now surface as an assistant chat card in the AiPanel with a violet **"Answer here (type or say it) — the popup will update."** hint. The user's next chat input (voice or text) is intercepted, dispatched as `axiom:voice-clarify-answer`, and the popup re-calls `/voice/actions/plan` with the answer baked into context. Backend `PlanIn` now accepts optional `clarification: str`; when present, the auto-injected "OR" question is suppressed. 53/53 pytest green (added `test_plan_passes_clarification_and_suppresses_or_question`).
- **Voice Actions — Phase 2: Meeting Links + Booking Page** — per-user meeting-link preference (Google Meet auto-generated / Zoom / Teams / Whereby / Custom / None) picked up by voice commands, plus a full Calendly-style **public booking page** at `/book/{slug}` reading the host's GCal free/busy. Bookings auto-create the GCal event with the visitor as an attendee — a Meet link is minted for Google Meet, embedded for static-link types. Free/busy is cached 5-min per (user,day) so a hammered public page won't burn Google quota. **11/11 pytest green**.
- **Voice Actions — Phase 1.5: Meeting Recap** — user rambles what happened after a call.
- **Voice Actions — Phase 1** — first slice of the AI voice-driven CRM overlay.
- **Modules Dropdown navigation style** — third nav layout users can pick under Settings.
- **Otter.ai** — fifth AI note-taker (Enterprise-only Bearer-token flow).
- **Grain OAuth 2.0 + PKCE (one-click)** — fourth AI note-taker.
- **Read.ai OAuth 2.1** — third AI note-taker (first OAuth-based).
- **tl;dv AI Note-Taker** — second provider on the pluggable `NoteTakerProvider` adapter (Fireflies is #1).
- **Tier 3 Google Workspace Integration** — Gmail **and** Google Calendar unlocked with one OAuth consent
  - `/crm/email` — Two-pane Gmail inbox (Inbox/Starred/Sent/Drafts/All Mail) with contact filtering, rich compose, threaded replies, attachments
  - `/crm/calendar` — Dedicated CRM month view with event compose (title, attendees, location, Google Meet, invite toggle)
  - Team Calendar — "Google on/off" toggle overlays Google events beside tasks/phases/time entries
  - Deal Drawer — "Schedule meeting" auto-invites the linked Contact + logs a meeting activity on the deal
  - Auto-refreshing tokens (`gmail_tokens` collection, per-user), `OAUTHLIB_RELAX_TOKEN_SCOPE=1` to tolerate previously-granted scopes


## What's Implemented (as of 2026-02)
- **Phase C polish: CRM Preset Templates (2026-02-28)** — One-click industry seed. Three presets rename the fixed 6-stage pipeline + seed activity kinds + lead sources without breaking any existing query/rollup/Deal→Project logic. Testing agent iteration_83 = **100% pass** on backend and frontend.
   • **Presets**: Field Service (Estimate Requested→…→Invoiced & Paid, with site_visit/quote_sent/photo_uploaded kinds and Google Ads/HomeAdvisor sources), Agency (Discovery→…→Retainer Signed, with proposal_sent/kickoff kinds and LinkedIn/award-listing sources), CPA / Accounting Firm (Inquiry→…→Active Client, with docs_requested/letter_sent kinds and Chamber/SBA sources).
   • **Backend**: `crm_settings` collection per company + `routes/crm_settings.py` with GET/PATCH/apply-preset. Stage KEYS stay fixed (lead/qualified/proposal/negotiation/won/lost) — only labels/kinds/sources vary. Deal and Contact activity endpoints now merge preset activity_kinds into their validation set so custom kinds like `site_visit` are accepted without touching those files' code. PATCH is defensive (unknown stage keys silently dropped, empty values ignored, list-type dedup).
   • **Frontend**: `/crm/settings` page with 3 preset cards + editable stage labels + activity_kinds + lead_sources. `useCrmSettings` hook (in-memory cache, invalidated on apply/save) is consumed by DealsBoard column headers, DealFormModal stage dropdown, DealDrawer activity-kind dropdown, and ContactCrmPanel activity-kind dropdown + lead-source datalist.
   • **Regression**: 2 pytest cases (catalogue+defaults, apply-preset+custom-kind+PATCH validation). Full suite 8+ tests green (crm_settings + deals + contact_crm + time_entries + team_calendar).
- **Phase C polish: Contacts Unification (2026-02-27)** — Every Contact record now carries CRM lifecycle stage + lead source + unified activity feed + deal history.
   • **Backend**: Extended `contacts` with `stage` (lead/prospect/active_customer/past_customer/inactive) and `lead_source`. New endpoints `POST /api/companies/{cid}/contacts/{xid}/activities` (kind+body validation, 404 on missing contact) and `GET /api/companies/{cid}/contacts/{xid}/crm-summary` (returns contact, deals[], stats {open/won/lost count+value}, activity_feed[] — merges contact-level + every deal's activities, sorted desc, deal-sourced items carry `source='deal' + deal_id + deal_title`). Hardened `PATCH /contacts/{xid}` with a strict whitelist (dropped arbitrary keys previously accepted — including `company_id`/`id` hijack risk) and stage validation.
   • **Frontend**: `ContactCrmPanel.jsx` mounts on `/contacts/:id` above the transaction drawer with 5 stage pills (single-click PATCH), controlled `lead_source` input (blur-persist, safe against mid-edit re-renders), open/won/lost KPI cards, deal history rows (click → DealDrawer), unified activity composer + feed with "via deal: …" attribution, and a "New deal" CTA that opens `DealFormModal` with the contact **locked** (dropdown disabled when `defaultContactId` is passed).
   • **Regression**: 2 pytest cases (test_contact_crm.py — happy path + whitelist/stage validation). Phase C Deals + Phase B-3 suites still green (16+ tests total).
- **Phase C kickoff: CRM Deals Pipeline + Kanban + Deal→Project handoff (2026-02-27)** — Testing agent iteration_81 = 100% pass.
   • **Backend**: `deals` collection (title, contact_id/name, stage, value, probability, expected_close_date, owner, source, notes, lost_reason, project_id, order, activities[]). Endpoints: CRUD, `/board` (grouped by stage with per-column count/value_sum/weighted_sum + top-level open totals), `/move` (DnD; supports `before_id`/`after_id` fractional-order inserts, auto-stamps stage_change activity, auto-flips probability to 100/0 on won/lost), `/activities` (append note/call/email/meeting), `/convert-to-project` (requires contact; seeds project name/contact/estimated_revenue/end_date/notes from deal, flips deal to won + probability 100, idempotent, 409 on dup project name unless override provided).
   • **Frontend**: `/crm/deals` Kanban board with 6 stage columns (Lead · Qualified · Proposal · Negotiation · Won · Lost), KPI strip (open count · open value · weighted pipeline · won this view), native HTML5 drag-and-drop between/within columns with insert markers, `DealFormModal` for creation, `DealDrawer` slide-over with inline editing, activity feed (per-kind icons), and green "Convert to project" CTA that navigates to the seeded project on success. Sidebar CRM link, CrmPlaceholder updated with a "Open deals board" CTA.
   • **Regression**: 2 pytest cases in test_deals.py (CRUD/board/move/activities/delete + conversion idempotency + 409 dup name). Plus Phase B-3 pytest still green (5/5). 14/14 total across all new suites.
- **Phase B-3 polish batch: Timer + Approvals + Command Palette + Calendar Drilldown (2026-02-27)** — Testing agent iteration_80 = 100% pass on all four.
   • **Timer Mode**: `TimeTimer.jsx` on `/team/time`. Start/Stop clock with running-state persisted to localStorage under per-company key `axiom_time_timer_v1::<cid>` (prevents cross-tenant leakage on company switch). Hours rounded to 0.01, 1-minute floor, 24-hour cap. Survives page refresh.
   • **Timesheet Approvals**: New `status` field on `time_entries` (`approved` | `submitted` | `rejected`), default `approved` for backward compat. New endpoints `/submit`, `/approve`, `/reject`, `/bulk-approve` with `_require_manager` gate (owner/manager via app-role, membership, or employee record). Rollup NOW filters `status=approved` so submitted/rejected entries never inflate Project P&L labor cost. `/team/approvals` UI has 3 tabs (Awaiting / Approved / Rejected), individual approve/reject with reject-note prompt, and bulk-approve. TimeEntryForm ships with a "Save as draft" checkbox that flips button label to "Submit for approval" and saves as `status=submitted`.
   • **Command Palette**: `CommandPalette.jsx` mounted globally in Layout. ⌘K / Ctrl+K anywhere opens it. `GET /api/companies/{cid}/search?q=...` returns unified results across customers, projects, invoices, bills, tasks, employees with prefix ranking. Text-only match fields (name, title, number, email, entity_label) — status/priority text intentionally excluded so a query like "open" doesn't match every open invoice. Debounced 180ms. Arrow/Enter navigation. Recents cached in localStorage (last 5).
   • **Calendar Drilldown**: Every chip on `/team/calendar` (task, phase start/end, time entry) is clickable → `CalendarEntityDrawer.jsx` slides in from the right with entity details + inline actions: task Mark-done/Delete, phase Open-project, time entry Delete. Deletions refresh the calendar without page reload.
   • **Regression**: 5/5 pytest cases in test_time_entries.py + test_team_calendar.py green.
- **Phase B-3 wrap-up: Team Calendar (2026-02-27)** — Single-grid overlay of Tasks, Phase deadlines, and Time entries. Month and Week views, per-teammate filter, prev/next/today nav.
   • **Backend**: `GET /api/companies/{cid}/team-calendar?date_from&date_to&employee_id?` returns `{tasks, phases (with denorm project_name), time_entries, employees, counts}` in one payload. Phase-window overlap logic handles start-only / end-only / spanning ranges. employee_id filter narrows tasks by assignee_user_id (resolved via `employee.user_id`) and time entries by employee_id; phases stay project-scoped. Bad dates 400; reversed range 400.
   • **Frontend**: `/team/calendar` renders a 42-cell month grid (leading/trailing adjacent-month days flagged), or a 7-cell week grid (Monday-anchored). Chip types: **task** (priority-tinted), **phase-start** (amber ▶), **phase-end** (rose ■), **time-logged** (emerald `Xh` aggregate on day cell + per-entry chips in week view). Today cell highlighted. Sidebar Team → Calendar link.
   • **Regression**: 1 pytest case (test_team_calendar.py — window/filter/reversed-range/bad-date/roster). Testing agent iteration_79 = 100% pass on backend and frontend, no bugs.
- **Phase B-3: Time Tracking — virtual labor cost roll-up (2026-02-27)** — Field employees log hours against a project/phase; labor cost = hours × cost_rate_snapshot rolls into the project view automatically. Design choices per user: (a) virtual/soft roll-up only — no GL journal entry, (b) no approval workflow (live immediately), (c) billable flag on every entry (future CRM invoicing), (d) day-by-day list UX (weekly grid deferred).
   • **Backend**: `time_entries` collection + `/api/companies/{cid}/time-entries` CRUD, plus `/rollup?project_id=` (totals + by_phase + by_employee with billable splits) and `/my-week?anchor=` (Monday-anchored 7-day window for personal summary strip). Cost rate is snapshotted at write time so future employee rate changes don't retro-affect historical entries. Validation: date YYYY-MM-DD, hours ∈ (0, 24], employee + project must exist in the company.
   • **Frontend**: `/team/time` (personal TimeLog page) has a weekly summary strip with prev/next/this-week nav, an inline `TimeEntryForm` (employee · project → phase · date · hours · billable · notes), and a day-grouped list of the last 30 days with project + billable filters. Every project's Detail page gets a new **Time** tab (`ProjectTimeTab.jsx`) with a pre-locked log form, 4 KPI cards (total hours · labor cost · billable hours · billable value), by-phase and by-employee rollup tables, and the full entry list with delete.
   • **Regression**: 2 pytest cases (test_time_entries.py — CRUD/validation/rollup/my-week + rate-snapshot isolation). Testing agent iteration_78 = 100% pass on backend and frontend; no blockers found. Fixed minor PATCH ordering nit reviewed by the testing agent (process project_id change before phase_id to avoid wasted lookup on mixed payloads).
- **Phase B-2: Polymorphic Notes + Employee Permission Overrides — regression verified (2026-02-27)** — Testing agent report iteration_77 pass; 15/15 backend pytest (test_notes, test_employees, test_tasks, test_notes_permissions_extended) plus in-browser Employee-modal Permissions/Notes tabs verified. Fixed 1 blocker found: WelcomeModal backdrop (`z-[900]`) previously intercepted pointer events on ProductRail — resolved by promoting the ProductRail nav to `relative z-[1000]`, so primary product navigation stays reachable even while a modal is open. Also tightened `permission_overrides` to a whitelist of known product keys (accounting/crm/team/projects) so junk keys never bleed into effective-permissions, and hardened the hard-delete employee guard to skip the assigned-tasks lookup when `user_id` is null (previously would false-match unassigned tasks).
- **Phase B-1: Team Foundation — Employees + Role Matrix (2026-02)** — First functional slice of the Team product.
   • **Backend**: `employees` collection with full CRUD (`GET/POST/PATCH/DELETE /api/companies/{cid}/employees[/{eid}]`) plus effective-permissions endpoint (`GET .../permissions`). Fields: name, email, phone, role (owner/manager/bookkeeper/field_employee), department, title, hourly_cost_rate, active, notes, permission_overrides, user_id (nullable — contractors don't need a login). Email uniqueness scoped per company. Role validation. Soft-delete by default, hard-delete blocked when tasks reference the employee. `ROLE_DEFAULTS` matrix: owner/manager see everything, bookkeeper sees accounting only, field_employee sees team+projects only. Overrides layer on top: `effective = defaults ∪ overrides`.
   • **Frontend**: `/team` route now renders real Team page (Employees directory) instead of the placeholder. List has color-coded role pills (amber Owner · violet Manager · cyan Bookkeeper · emerald Field employee), title/dept, hourly cost rate, archive/restore, and edit. Employee modal supports create + edit with role-defaults hint banner ("Role defaults — determines default product access: …"). Team sidebar item points to /team, old CompanyTeam page relocated to /company-team.
   • **Regression**: 1 pytest scenario covering CRUD + email dup guard + role validation + effective-permissions computation with overrides + soft/hard-delete. Passes locally.
- **Phase A-2: Product Rail + Per-Product Sidebar (2026-02)** — Full 4-product shell shipped.
   • **ProductRail component** (60px dark bar on far left) with 4 icon+label buttons: Accounting (cyan), CRM (violet), Team (emerald), Projects (amber). Active product derived from URL prefix via `detectProduct()`. Each button color-accented in active state via a static Tailwind class map (JIT-safe).
   • **Sidebar refactored to be product-aware** — reads `detectProduct(loc.pathname)` and branches its render into 4 mini-navs. Accounting keeps its full grouped nav (minus Projects, which was extracted). Projects gets its own sidebar (All projects, Estimates vs Actuals) with a friendly amber banner. CRM and Team show minimal starter navs with color-coded "coming in Phase B/C" banners.
   • **Placeholder pages** `/crm` and `/team` render feature-preview cards showing what's coming (Deal pipeline / Contacts view / Activities / Notes / Calendar for CRM; Employees / Assignments / Time tracking / Team calendar / Announcements for Team). Users see the vision and know where they are before Phases B/C ship.
   • Layout composes `<ProductRail /> + <Sidebar /> + <main>`. Global Tasks button, AI panel, and company switcher work across every product.
- **Phase A-1: Global Tasks (2026-02)** — First shipped piece of the multi-product platform vision.
   • **Backend**: `tasks` collection with full CRUD + toggle-complete endpoint (`GET/POST/PATCH/DELETE /api/companies/{cid}/tasks[/{tid}]`, `POST .../complete`). Fields: title, description, assignee_user_id, created_by_user_id, due_date, status (open/done/cancelled), priority (low/med/high), entity_type + entity_id + entity_label (polymorphic ref to any parent doc — invoice, bill, project, phase, deal, employee, transaction). Filter query param supports `all / open / today / overdue / mine / done` plus `entity_type + entity_id` for per-entity task rollups later.
   • **Frontend**: `GlobalTasksButton` in the top bar (visible on every product/page), shows a live count badge polling every 60s. Clicking opens a right-side drawer (z-[85], above AI panel) with: filter chips (Open/Today/Overdue/Mine/Done), smart-grouped list by due-date bucket (Overdue · Today · This week · Later · No due date), per-task checkbox to toggle done, inline delete on hover, entity-link that jumps to the source doc. Quick-add footer with title + due date + priority. Keyboard shortcut ⌘⇧T opens the drawer from anywhere.
   • **Regression**: 1 pytest scenario covering CRUD + all 5 filter modes + entity filtering + toggle-complete round-trip. Passes locally.
   • **Strategic direction locked**: 4-product platform (Accounting exists · CRM new · Team new · Projects promoted from accounting nav). Product-switcher rail + per-product sidebar shell = Phase A-2. Phase B = Team foundation (employees as users with role-based permissions + per-employee overrides). Phase C = CRM core (leads → deals → conversion to projects). Phase D = cross-cutting AI/search/notifications. Every product will be AI-native. Employees are users with configurable product access. Both CPA firm and each client company have their own CRM.

## What's Implemented (previously)
- **Project + Phase Modal Forms (2026-02)** — Both entities now use full-featured modals instead of cramped inline forms.
   • **Backend**: Phase create/update now accepts `notes`, `estimated_revenue`, `estimated_cost`. Empty string or null clears; otherwise coerced to float. Regression: all 8 phase/project pytest scenarios pass (3 new for the new fields + 5 existing).
   • **Project modal** (`ProjectFormModal`): fields Name (required), Customer (required), Estimated $, Start / End dates, Notes/description. Dismisses via X, Escape, or backdrop click. Submit disabled until required fields present + end date not before start.
   • **Phase modal** (`PhaseFormModal`): fields Name (required), Start / End dates, Estimated revenue, Estimated cost, Notes, plus a **Linked Documents** section that after first save reveals (a) a list of unlinked estimates/invoices/bills for the project's customer with one-click "Link" button that PATCHes `phase_id + project_id` onto the doc, and (b) three "New estimate / invoice / bill" buttons that close the phase modal and open the DocDrawer with `phaseId` pre-filled in the Phase picker.
   • Existing phase rows now have an edit-pencil that opens the same modal in edit mode with all fields pre-populated. DocDrawer accepts `phaseId` to pre-fill the Phase picker when creating a doc from within the phase modal.
   • Testing agent verified 100% pass (8/8 backend + all critical frontend flows).
- **Project Documents — In-Drawer Editor (2026-02)** — Full-featured Invoice/Bill/Estimate editing without leaving the ProjectDetail page.
   • Refactored `InvoiceEditor`, `BillEditor`, `EstimateEditor` to accept an optional `embed` prop: `{ projectId, invoiceId|billId|estimateId, onSaved(id), onClose }`. When embedded, the editor derives its doc-id from props (not `useParams`), skips URL-based query-param prefill in favor of the `projectId` prop, no-ops the `navigate` shim, and calls `embed.onSaved(newId)` after create/duplicate instead of pushing a URL. Back button hidden in embed mode. Route-mode behavior 100% unchanged.
   • ProjectDetail Documents tab now opens a right-side slide-over drawer (`z-[80]`, above the AI panel) instead of navigating. Header shows "NEW/EDIT {KIND} · This project" + a "Full page" escape hatch button that closes the drawer and routes to the standalone editor for advanced needs. Drawer dismisses via X, Escape, or scrim click. On save, the docs table refreshes in the background and the drawer switches into edit mode so the user can continue polishing (attachments, PDF preview, send-email, follow-ups all remain available inline).
   • Testing agent verified 12/12 frontend scenarios (drawer new/edit for all 3 kinds, save round-trip, all 3 dismiss paths, full-page escape hatch, plus route-mode regression on `/invoices/new`, `/bills/new`, `/estimates/new`). Zero P0/P1 regressions.
- **Projects — Top-Level Nav (2026-02)** — Projects promoted out of the Accounting group; now sits between Contacts and Accounting as its own top-level entry (gated by `projectsEnabled`).
- **Budgets — Scope Filters (2026-02)** — Extended Phase 4 so a shop can budget separately per Class or per Project instead of only company-wide.
   • Backend: `POST /budgets` accepts `scope` (`company` | `class` | `project`) + `scope_ref_id`; new `_resolve_scope` helper validates the ref exists AND the matching feature flag is enabled (400 with helpful detail otherwise). Denormalized `scope_ref_name` stamped on the doc for cheap list rendering. Uniqueness key now `(fiscal_year, scope, scope_ref_id, lower(name))` so the same name can coexist at Company + Class + Project scope in the same FY. PATCH rename enforces the same scope-aware constraint. Prefill + variance report both thread `class_id`/`project_id` (derived from `budget.scope_ref_id`) into `_signed_balances` — same engine as class-sliced P&L so numbers match to the penny.
   • Frontend: (1) Budgets list quick-create gains a Scope dropdown with conditional Class or Project picker; New button gated until a ref is picked. Class/Project options only render when the matching feature flag is on. (2) List rows show a scope pill (slate for Company-wide, cyan for Class, emerald for Project) with the referenced name. (3) BudgetEditor header shows the same pill alongside status. (4) Prefill modal copy mentions the scope filter. (5) BvA report shows a scope banner above the grid explaining "Only postings tagged with this X count as actuals"; company-scoped budgets omit the banner. Budget selector option text now suffixes `· class: Marketing` / `· project: Kitchen Remodel` for scoped budgets.
   • Regression: 4 new pytest scenarios in `tests/test_budgets_scope.py` (create + uniqueness across scopes, feature-flag guard, scoped variance filter, scoped prefill filter). All 8/8 Budgets tests pass + testing agent verified live end-to-end.
- **Budgets — Phase 4 (2026-02)** — Spreadsheet-style planning + variance report shipped.
   • Backend: `budgets` CRUD (`GET/POST/PATCH/DELETE /api/companies/{cid}/budgets[/{id}]`) with fiscal-year-scoped name uniqueness, status vocabulary (`draft`/`active`/`archived`), and cascade-delete of `budget_lines` on hard-delete. `PUT /budgets/{bid}/lines` bulk-upserts (`{lines: [{account_id, period_key, amount}]}`) — `amount=0` clears (delete), non-P&L account or out-of-year period → 400, unknown account silently skipped. `POST /budgets/{bid}/prefill` copies FY-1 monthly actuals via the exact same `_signed_balances` engine as P&L reports (perfect parity), with optional compounded `growth_pct`. `GET /reports/budget-vs-actuals?budget_id=X&basis=accrual|cash` returns per-account rows grouped by revenue / cogs / expenses with 12 monthly triples (budget, actual, variance) + full-year totals + net-income summary. **Variance is pre-signed** so "positive is always good" on both sides (revenue: `actual - budget`, expense: `budget - actual`).
   • Frontend: (1) `/accounting/budgets` list page mirrors Classes/Projects shape with quick-create (name + fiscal_year) and status dropdowns. (2) `/accounting/budgets/{bid}` spreadsheet editor — sticky left "Account" column, 12 monthly columns + Total, grouped by section (Revenue / COGS / Expenses) with section-subtotal rows, dirty-cell violet highlight, bulk Save button showing dirty count, and Enter-to-fill-row shortcut. Prefill modal with growth uplift %. (3) `/reports/budget-vs-actuals` variance report — stacked-format cells (Bud · Act on top / Variance colored bottom) so all 12 months fit at 1920px without collision, budget & basis selectors persist via URL. (4) Sidebar "Budgets" entry gated by `features.budgets_enabled`. (5) Reports landing tile for BvA. (6) Settings toggle flipped from "Coming soon" to "Available".
   • Regression: 4 pytest scenarios (CRUD + dup guard, bulk upsert with P&L guard and clear semantics, variance sign convention w/ revenue+expense, prior-year prefill w/ 10% growth uplift) in `tests/test_budgets.py`. Testing agent added 6 more validation scenarios (`test_budgets_validation.py`); 10/10 pass end-to-end.
- **Project Timeline + Doc Linking — Phase 3.5 (2026-02)** — 3-tab ProjectDetail with dates, Gantt, and one-click linked docs.
   • **ProjectDetail tabs**: Overview (existing P&L + Phases), **Timeline** (project + phase date pickers + CSS-grid Gantt chart with status-colored bars and legend), **Documents** (roll-up of every linked estimate / invoice / bill / receipt with quick-create buttons that deep-link the editor).
   • **Doc linking**: `InvoiceCreate` / `BillCreate` / `EstimateCreate` accept `class_id` / `project_id` / `phase_id`; invoice/bill/estimate POST persists them; PATCH pass-through (raw `$set`). New reusable `ProjectPhaseClassPicker` component embedded in all three editors, gated by `classesEnabled` / `projectsEnabled`. Editor pre-fills project + customer from `?project_id=<id>` on new-mode.
   • **New endpoint**: `GET /api/companies/{cid}/projects/{pid}/documents` returns per-doc `{id, kind, number, date, contact_name, total, balance_due, status, phase_id}` across estimates / invoices / bills / receipts, sorted by date desc. 404 on unknown project.
   • **Projects page**: quick-add gains Start + End date inputs so PMs can bookmark timeframes at create time.
   • Regression: 3 pytest scenarios (project + phase date round-trip, invoice/bill/estimate `project_id` on create + PATCH, `/documents` cross-type rollup) in `tests/test_project_timeline_docs.py`. All 22 project-related tests pass end-to-end.
- **Project Phases — Live Management (2026-02)** — Phases page + rollup are now fully wired for daily use.
   • **ProjectDetail** phase rows gained an inline **rename** (pencil ✎ button → inline input with Enter to save, Esc to cancel) and a per-row **status dropdown** (Planning · In progress · On hold · Completed · Cancelled). Completed phases render at 60% opacity with a "done" badge. Metadata merged from `/phases` fetch so status renders alongside the P&L numbers from `by_phase`.
   • **Txn rollup drawer** now offers a **phase picker** that cascades off the project picker — visible only when the txn has a `project_id` AND the parent project has phases. Changing the parent project auto-clears the phase (enforces one-project-per-phase invariant). Phase list lazy-loaded per project into a `phasesByProject` cache so subsequent rows render instantly.
   • Also converted the drawer-based profitability view to a dedicated `/accounting/projects/:projectId` route with breadcrumb navigation (`← Projects › <name>`) so PMs can bookmark, share, and back-nav.
   • No new backend surface — everything uses the existing `PATCH /phases/{id}` and `PATCH /transactions/{id}` endpoints.
- **Project Phases + Per-Phase P&L (2026-02)** — Long jobs now bucket their P&L by phase (Demo → Framing → Finishes).
   • Backend: full phase CRUD nested under a project (`GET/POST/PATCH/DELETE /api/companies/{cid}/projects/{pid}/phases[/{id}]`) with project-scoped case-insensitive name uniqueness, auto-appending `sort_order`, and a guarded hard-delete that blocks when any txn / JE line / doc still carries the phase. Deleting a project cascades to its phases. Profitability endpoint gains `?group_by_phase=1` which walks project-scoped txns + JE lines once, attributes each posting to its `phase_id` (or a synthetic "Unphased" bucket when unset), and returns per-phase `revenue / cogs / expenses / net_income` alongside the existing top-level totals — phase totals sum to the top-level totals.
   • Frontend: Projects page profitability drawer gets a **Phases** section — inline add (Enter to submit), per-phase P&L table (revenue / cost / net with color-coded net), and per-row delete with usage guard. "Unphased" row surfaced in italic when the project has un-tagged postings so nothing goes missing. Loads phases + profitability in parallel on drawer open.
   • Regression: 2 pytest scenarios (phase CRUD with in-use guard + per-phase P&L breakdown vs top-level totals) in `tests/test_project_phases.py`. All 26 advanced-features tests pass.
- **Estimates vs Actuals Report (2026-02)** — Full per-project commitment / paid / remaining dashboard for Project Managers.
   • Backend: `GET /api/companies/{cid}/reports/estimates-vs-actuals` bulk-fetches every project-linked invoice + bill in two queries (cheap even at 10k+ rows), rolls up into per-project metrics: `estimated`, `invoiced`, `received` (from invoice `balance_due`), `ar_outstanding`, `committed` (bills), `paid_to_vendors`, `ap_outstanding`, `net_cash` (received - paid), plus convenience `pct_billed` / `pct_collected`. Grand-totals row included. `?include_completed=0` hides finished projects; cancelled always hidden.
   • Frontend: new `/reports/estimates-vs-actuals` page renders as a wide 8-column table (Project · Estimated · Invoiced · Received · Remaining · Committed · Paid · Net cash) with a totals footer, an "Include completed" filter, an inline "Enable Projects" fallback for deep-linked users, and per-cell tooltips explaining every formula. Tile added to the Reports landing page — only rendered when `features.projects_enabled`, keeping the grid uncluttered for shops that don't track per-job P&L.
   • Regression: 2 pytest scenarios (multi-project rollup with mixed AR/AP + include_completed filter) in `tests/test_estimates_vs_actuals.py`. All 24 advanced-features tests pass end-to-end.
- **Advanced Features — Phase 3: Projects (2026-02)** — Time-bound customer-job tracking now shipped. Backend: full `projects` CRUD (`GET/POST/PATCH/DELETE /api/companies/{cid}/projects[/{id}]`) with mandatory `contact_id` (customer), lifecycle status (`planning`/`in_progress`/`on_hold`/`completed`/`cancelled`), estimated_revenue + hourly_cost_rate, per-customer name uniqueness, soft-delete (status→cancelled) + guarded hard-delete. New report endpoint `GET /companies/{cid}/reports/project-profitability?project_id=X` runs the existing `_signed_balances(project_id=…)` engine, splits into revenue/COGS/expense buckets, computes gross + net, and joins `estimated_revenue` to return `pct_of_estimate`. Frontend: (1) new `/accounting/projects` page mirroring the Classes shape with quick-add (name + customer + estimate), status dropdown per row, and a right-side profitability drawer with revenue/COGS/expense breakdown and an "N% earned" progress bar against the estimate, (2) new "Projects" sidebar entry gated by `features.projects_enabled`, (3) `useCompany()` exposes `projectsEnabled`, (4) per-txn project dropdown in the rollup drawer sits next to the class dropdown when the flag is on, (5) Settings toggle for Projects flipped from "Coming soon" to "Available", (6) onboarding nudge on the Advanced Features card — Construction and Professional Services industries get a soft blue banner with a one-click "Enable Projects" that dismisses to localStorage after use. Regression: 3 pytest scenarios (CRUD happy path, in-use hard-delete guard, profitability rollup with JE + txn contributions) in `tests/test_projects_crud.py`. All 22 advanced-features tests pass end-to-end.
- **Class-Sliced Reports + QBO Class Read-Sync (2026-02)** — Two Phase 2 follow-ups shipped together.
   • **Class filter chip on Reports**: P&L / BS / Cash Flow endpoints now accept `?class_id=<id>` and slice their signed-balance query through the same class filter the rollup uses. Backend: `compute_income_statement` / `compute_balance_sheet` / `compute_cash_flow` gained an optional `class_id` kwarg threaded down to `_signed_balances` (transactions layer) and native cash-flow txn query. Cache key includes class so switching classes doesn't blow the un-filtered cache. Frontend: `ReportView.jsx` adds a "Class" select next to the basis toggle, only rendered when `features.classes_enabled` AND the current report is one of the three — with a "clear" affordance and URL param persistence via `useSearchParams`.
   • **QBO Class Read-Sync**: `_flatten_lines` and `map_journal_entry` now capture `ClassRef` on every imported line as `qbo_class_id` + `qbo_class_name`. New module `qbo_class_sync.py` runs a two-pass resolver: (1) walks every doc under a company and mints an Axiom `classes` row per unique QBO class (case-insensitive name match to adopt existing manual rows, `source="qbo"` + `qbo_id` stamped), (2) stamps `class_id` on the parent doc (first-line rule, matches QBO's own P&L-by-class rollup) and per-line on `journal_entries.lines[i].class_id`. Auto-runs at the end of `run_migration()` — silent, non-blocking. Manual re-trigger via `POST /api/companies/{cid}/classes/sync-qbo` for companies migrated pre-Feb-2026.
   • Regression: 7 new pytest scenarios (4 QBO class sync + 3 class-sliced reports) in `tests/test_qbo_class_sync.py` and `tests/test_class_sliced_reports.py`. All 19 advanced-features tests pass end-to-end.
- **Advanced Features — Phase 2: Classes UI (2026-02)** — First user-visible advanced feature. Backend: `PATCH /api/companies/{cid}/features` for flag flips, full `classes` CRUD (`GET/POST/PATCH/DELETE /api/companies/{cid}/classes[/{id}]`) with case-insensitive name uniqueness, 1-level parent nesting cap, soft-delete + guarded hard-delete (blocks when any txn / JE line / doc references the class), and `TransactionUpdate.class_id` (+ project/phase) with empty-string-clears-to-null semantics. Frontend: (1) new **Advanced Features** tab in Settings with per-flag toggle rows + coming-soon states for Projects & Budgets, (2) new `/accounting/classes` page with quick-add, inline rename, archive/restore, hard-delete, and a "Classes aren't enabled yet" empty-state on deep-link, (3) new "Classes" sidebar entry gated by `features.classes_enabled` via the existing `advancedOnly`-style filter, (4) per-txn class dropdown inside the rollup drawer (only rendered when the flag is on — zero clutter for companies that don't use classes). `useCompany()` now exposes `classesEnabled` / `projectsEnabled` / `budgetsEnabled` boolean shortcuts. Regression: 5 pytest scenarios in `tests/test_classes_crud.py`.
- **Advanced Features Foundation — Phase 1 (2026-02)** — Invisible foundation for Classes / Projects / Budgets. `companies.features` sub-doc with three OFF-by-default flags (`classes_enabled`, `projects_enabled`, `budgets_enabled`). New collections skeleton: `classes`, `projects`, `project_phases`, `budgets`, `budget_lines`. All nullable FKs (`class_id`, `project_id`, `phase_id`, plus `qbo_legacy_tags` for 2028-sunset Tags capture) added to `transactions`, `invoices`, `bills`, `payments`, `receipts`, `estimates`, and `journal_entries.lines`. Sparse indexes on every FK, plus a unique `(budget_id, account_id, period_key)` index. `reports.py::_signed_balances` gains optional `class_id` / `project_id` filters (default None → zero query cost change). Startup hook backfills `features` on legacy companies. See `/app/backend/advanced_features.py` for the full taxonomy. Regression: 7 pytest scenarios in `tests/test_advanced_features_phase1.py`.
- **Industry Template Switch + Safe CoA Cleanup (2026-02)** — During onboarding a user can switch industry templates and the CoA now adjusts safely.
   • `POST /api/companies/{cid}/industry-template` extended: `dry_run=true` returns `{would_add, would_remove, blocked_remove}`; `confirm_cleanup=true` seeds new + removes old.
   • Every seeded account is stamped `seeded_by_industry=<slug>` so future switches can distinguish template-seeded rows from manually-added ones. Codes in the new template that pre-existed unstamped get back-filled on save.
   • Removal is strictly scoped: only accounts stamped for the OLD industry AND absent from the NEW template AND unreferenced by any transaction/JE line/rule. Manually-added and referenced accounts are always preserved (returned in `blocked_remove`).
   • Company doc gains `industry_selected_at` timestamp alongside `industry_template`.
   • Frontend `IndustryTemplatePicker` shows a confirmation modal listing add/remove/kept before committing. Additive path unchanged when there is no prior template.
   • Regression tests: 4 scenarios (first pick stamps, switch removes industry-only, in-use accounts blocked, manual accounts never touched) in `tests/test_industry_template_switch.py`.
- **Global Contact Directory shipped (2026-02)** — Curated 5,221 well-known merchants across 15 verticals via Claude, merged with specificity-wins conflict resolution (258 conflicts auto-resolved), and integrated as a new tier in the Standard+ cascade. Data lives at `/app/data/global_directory/merged/global_contact_directory.json`. New module `backend/global_contact_directory.py` loads 19,896 aliases into memory at boot for O(1) lookup with prefix support for memo-suffix noise (e.g., `STARBUCKS #1234 SEATTLE WA` → Starbucks). `contact_resolver.py::resolve_contact` and `resolve_contacts_batch` now check the directory on tenant-miss, mint contacts under CANONICAL names (dedupes "Starbucks Coffee" vs "Starbucks"), and attach `logo_url` (Clearbit) + `linked_semantic` at creation. `plaid_connect.py` stamps `category_hint_semantic` + `category_hint_source` on the txn. `standard_plus_categorizer.py` reads the hint as Stage 2 of the cascade — placed AFTER the 485 hand-tuned Global Vendor Rules (rule wins on Home Depot/Kroger/etc. where the hand-tune is more SMB-appropriate) but BEFORE Plaid PFC (broad tail coverage). Provenance UI dot: violet for `standard_plus_directory`. 53 regression tests pass including rule-beats-directory and directory-fires-when-no-rule.
- **AI-First (Beta) Retired (2026-02)** — Removed the failed AI-First pipeline. Zero companies were on `ai_first` mode at removal. Deleted `ai_first_categorizer.py`, `test_ai_first_clustering.py`, and the `/companies/{cid}/ai-first/categorize-batch` endpoint. Tightened the categorization-mode validator to accept only `standard | standard_plus`. Removed AI-First branches from `sync_tasks.py` and `statements.py`. Removed the AI-First radio option from Settings. Historical rows tagged `categorization_source == "ai_first*"` still render on the Transactions page with a neutral "(legacy — retired)" provenance label so no data appears broken. Defensive DB sweep normalized any `categorization_mode: ai_first` companies to `standard` (0 affected). End-to-end verified: `POST /categorization-mode {"mode":"ai_first"}` → HTTP 400, `standard_plus` → HTTP 200, old endpoint → HTTP 404.
- **Standard+ Phase 2e — Name-First Semantic Resolution (2026-02)** — P0 fix for the "Domino's in Insurance" bug. Standard Plus LLC had a non-standard CoA where `6400 = Insurance` (not Meals). The old code-first resolver blindly translated semantic "meals" → code 6400 via the generic template map, landing Domino's in Insurance. Rewrote resolution to **name-first**: `SEMANTIC_TO_NAME_PATTERNS` (28 semantic keys, ordered most-specific-first) is scanned against the company's actual CoA account names; only when no name pattern matches does it fall back to code-based lookup. New helper `global_vendor_rules.resolve_semantic_to_account(semantic, accounts, template)` returns the actual account dict. `standard_plus_categorizer.py` now calls this instead of the old `code_to_acct[match["account_code"]]` lookup. 8 new regression tests in `tests/test_standard_plus.py::TestSemanticNameFallback` including the exact Domino's-on-swapped-CoA scenario. All 38 Standard+ tests pass.

- **Plaid Delayed Backfill Poller (2026-08-26)** — Fixes a P0 where freshly-linked Plaid items sometimes stalled at ~30 days of history despite `days_requested=730` being sent correctly on the link token. Root cause: Plaid's `/transactions/sync` returns only the currently-cached window at connect time (~30 days), and in the newer sync-mode webhook flow the follow-up `SYNC_UPDATES_AVAILABLE` webhook that carries the backfill may fire once or not at all. Fix: after `/onboarding/plaid/import` succeeds, enqueue a self-scheduling `plaid_delayed_backfill_sync` task that re-runs `_run_sync` at +30s, +2m, +5m, +15m, +30m per item. Stops early when we've reached the requested `import_start_date` floor OR a real `HISTORICAL_UPDATE` webhook stamps `historical_update_received: True`. Final attempt sends `trigger="HISTORICAL_UPDATE"` so opening-balance JEs still land even when Plaid never fires the webhook. Regression tests in `tests/test_plaid_delayed_backfill.py`. Recovered stuck company AI First 2 LLC from 89 → 1980 txns (18 months) via manual replay.
- **AI-First Speed Bundle + Cluster Categorization (2026-08-26)** — Rebuilt `ai_first_categorizer.py` around Puzzle's cluster-then-propagate pattern PLUS the three highest-ROI performance dials.
- **Standard+ Beta Categorization Pipeline (2026-08-26)** — Third categorization mode alongside Standard and AI-First. Approach borrowed directly from Puzzle/Ramp/Brex: deterministic Global Vendor Rules layered over the untouched Standard cascade.
- **Standard+ Phase 2 — Plaid PFC → Semantic Fallback (2026-08-26)** — Extended Standard+ cascade with a second stage that reads Plaid's Personal Finance Category taxonomy (~104 canonical categories that Plaid attaches to every transaction).
- **Standard+ Phase 2b — Amount-Bucket Rules (2026-08-26)** — Closed the last real accuracy gap between Standard+ and Standard on ambiguous merchants. Same design pattern as AI-First's clustering: rules can now declare per-bucket (`s` / `m` / `l` / `xl`) semantic + confidence overrides, so `$8 Costco → Meals @ 0.75` and `$850 Costco → Supplies @ 0.75` are treated as different categorization decisions with zero LLM cost.
- **Standard+ Phase 2c — Tenant Priority Guard (2026-08-26)** — Fixed a correctness bug where the Global 485 rules could silently clobber per-tenant customizations. Now Standard+'s post-hook checks each row's `ai_source` and skips override when Standard already applied a customer-specific rule (`ai_source == "rule"`) or a hit from the customer's own merchant memory (`ai_source == "memory"`). Priority stack now enforced: `Tenant Custom > Tenant Rules Miner > Tenant Merchant Cache > Global 485 > Plaid PFC > LLM`. New `skipped_tenant_priority` stat exposed on the API response. Regression tests in `tests/test_standard_plus.py::TestTenantPriorityGuard` (3 tests: rule wins, memory wins, LLM-sourced overridable). Standard LLC benchmark unchanged (zero rows had tenant-priority sources to begin with).
- **Standard+ Phase 2d — Retroactive Re-categorize UI (2026-08-26)** — When a user flips a company to Standard+ mode, they can now retroactively apply the Global Rules + PFC fallback to every existing transaction with one click. Prior to this, flipping the mode toggle only affected new incoming rows.
- **Provenance Dots on Transactions (2026-08-26)** — Layer 1 of the Rule Provenance UX. Small 4px colored dot beside each transaction's category on the Transactions page indicating which tier of the cascade decided the answer. Hover tooltip shows the full breakdown: source label · matched pattern · semantic key · amount bucket · confidence.
   • Color mapping: emerald = tenant's own decision, blue = Standard+ Global Rule, cyan = Plaid PFC, amber = LLM guess, gray = unknown.
   • **Toggled off by default** via new `companies.show_categorization_source_badges` field (Settings → "Show categorization source badges" checkbox). Advanced-CPA UX — hidden from end-users who prefer clean books until they opt in.
   • Reads both new-style (`categorization_source`) and legacy (`ai_source`) fields so all three categorization modes (Standard / AI-First / Standard+) render correctly.
   • Backend: `show_categorization_source_badges` added to `_ALLOWED_UPDATE_FIELDS` on `PATCH /companies/{cid}`. Curl-verified round-trip.
   • Files: `frontend/src/components/ProvenanceDot.jsx` (new), `AIFirstControls.jsx` (added toggle component), `Transactions.jsx` (added conditional render + flag fetch on mount).


   • Backend: `POST /companies/{cid}/standard-plus/apply-rules` now accepts `{"all": true}` and internally fetches every txn ID for the company (no need for the frontend to pre-fetch and post a 2,000-item array).
   • Frontend: new `StandardPlusApplyButton` component in `AIFirstControls.jsx` — only visible when mode == `standard_plus`. Confirmation modal explains behavior (idempotent, tenant-priority-safe). Toast on success reports scanned/overridden/rule/PFC/preserved counts.
   • Curl-verified on Standard LLC: 1,983 scanned, 1,824 matched (1,129 rule + 695 PFC), 1,803 overridden, 0 tenant priority conflicts.




   • **Buckets** (mirror AI-First): `<$50` = s, `$50-500` = m, `$500-5000` = l, `$5000+` = xl. `amount_bucket()` helper uses `abs(amount)` so refunds bucket correctly.
   • **21 merchants upgraded** to amount-aware: Walmart, Wal-Mart, Costco (whse/wholesale/generic), Sam's Club, BJs, Target, Amazon.com, Amazon Marketplace, AMZN MKTP, Home Depot, Lowe's/Lowes, Best Buy, Apple Store, 7-Eleven, Wawa, Sheetz, Circle K, CVS Pharmacy (both spellings), Walgreens, Rite Aid, Duane Reade.
   • **Live benchmark** on Standard LLC's 1,983 rows: 417 rows now resolved by amount-aware rules; deterministic backing steady at 91%; needs_review dropped slightly (1,401 → 1,384) as some buckets resolve above the 0.75 trust threshold; zero LLM cost added.
   • Every categorization now stamps `rule_matched` + `bucket` for auditability — CPA can see exactly which cell of the rule table drove the decision.
   • Regression tests in `tests/test_standard_plus.py::TestAmountBucketRules` (8 tests: small→meals, large→supplies, Home Depot construction override, absolute-value bucketing, no-amount fallback, gas station split, bucket cutoff parity with AI-First).


   • **`pfc_semantic_map.py`** — maps every Plaid PFC detailed key (FOOD_AND_DRINK_COFFEE, TRAVEL_LODGING, LOAN_PAYMENTS_CREDIT_CARD_PAYMENT, etc.) to the same semantic keys used by Global Vendor Rules. Template-aware resolution via `resolve_semantic` gives industry-specific account codes for free.
   • **Confidence tiering**: Plaid's `confidence_level` field → 0-1 float (VERY_HIGH→0.90, HIGH→0.85, MEDIUM→0.70, LOW→0.55, UNKNOWN→0.65). Feeds the same tri-state gate used elsewhere.
   • **Priority in cascade**: Merchant rule beats PFC (rules are more specific and higher-confidence). Only rows where Global Rules didn't match consult PFC.
   • **Ingest plumbing**: `plaid_connect.categorize_and_insert_plaid_txns` now also persists `pfc_confidence_level` on each txn (already had `pfc_detailed` + `pfc_primary`) so the fallback stage sees Plaid's actual confidence.
   • **Live benchmark** on Standard LLC's 1,983 rows: coverage jumped from **56% → 91% deterministic backing** (1,108 rule matches + 695 PFC matches = 1,803 rows). Uncategorized still at 90 rows (5%) — same as before because PFC doesn't reduce uncategorized further, it just replaces Standard's category with a more precise one on more rows. Zero LLM calls added.
   • Regression tests in `tests/test_standard_plus.py::TestPfcFallback` (6 tests: PFC resolution, confidence mapping, unmapped PFC skip, rule-beats-PFC ordering, PFC-fallback end-to-end).
   • Phase 3 backlog: MCC code cross-reference, industry-specific rule variants (Home Depot → COGS for construction), vector-similarity fallback (Mongo Atlas Vector Search).


   • **`global_vendor_rules.py`** — v1 draft of 485 curated US merchants (coffee, QSR, sit-down, delivery, rideshare, airlines, hotels, rental cars, gas stations, big-box, home improvement, grocery, pharmacy, office/tech, SaaS, payroll, payment processors, credit-card issuers, bank fees, telecom, utilities, insurance, mortgage/car loans, advertising, marketplace fees, shipping, gov/taxes, streaming, gyms, ACH keywords). Semantic-key indirection (`meals`, `software_saas`, `utilities`, `loan_payment`, etc.) → per-template account-code mapping so the same rule works across generic / professional_services / restaurant / ecommerce / construction.
   • **`standard_plus_categorizer.py`** — post-hook that runs after Standard's cascade completes on inserted rows. Tri-state confidence gate identical to AI-First: `≥0.75` apply + `needs_review=False`, `0.50–0.75` apply + `needs_review=True`, `<0.50` don't override (Standard's answer stands). Contact/AI-confidence/reasoning from Standard preserved.
   • **Routing**: three-value `categorization_mode` (`standard` | `ai_first` | `standard_plus`). Standard cascade code path is 100% untouched. Post-hooks branch in `sync_tasks._run_sync` (Plaid path) + `statements.py` (statement path) mirror each other.
   • **Frontend**: `AIFirstControls.CategorizationModeToggle` extended to a third option; `POST /companies/{cid}/categorization-mode` accepts `standard_plus`; `POST /companies/{cid}/standard-plus/apply-rules` for retroactive re-categorization; `GET /global-vendor-rules/stats` for coverage metadata.
   • **Live benchmark** on Standard LLC's 1,983 real BofA rows: 1,108 overridden by rules (56% coverage), Uncategorized dropped 201 → 90 (-55%), runtime <2 seconds, cost $0. Decisively beats both Standard alone and AI-First on every real metric.
   • Regression tests in `tests/test_standard_plus.py` (13 tests: match ordering, tri-state confidence, template fallback, missing merchant handling, override behavior).
   • Phase 2 backlog: Plaid PFC → CoA mapping, MCC code cross-reference, industry-specific rule variants, vector-similarity fallback (Mongo Atlas Vector Search).


   • **Cluster-based propagation**: Group by `(canonical_merchant, amount_bucket, direction)`. Only ONE representative per cluster hits the LLM; results propagate to every sibling above `_PROPAGATE_MIN_CONFIDENCE = 0.75`, else the cluster is flagged needs_review together. Amount buckets prevent Costco-food-court-vs-bulk-supplies cross-contamination; ACH/wires/checks are forced to solo clusters. Real benchmark on AI First 2 LLC's 1591 rows: **3.0x compression** (1591 → 537 clusters).
   • **`_LLM_CONCURRENCY = 24`** (was 8) via `asyncio.Semaphore` — 3x wave-count reduction.
   • **`_REPS_PER_LLM_CALL = 60`** (was 30) — 2x fewer round-trips per chunk.
   • **Anthropic prompt caching** via LiteLLM structured system-block with `cache_control: ephemeral` on the CoA + contacts + few-shots (easily >1,024 tokens). Live-verified: **46% latency drop** on cache-hit calls, matching Anthropic's documented ~50%. Universal-key routing preserved via emergentintegrations' `initial_messages` pass-through.
   • Combined impact on the AI First 2 LLC 1591-row scenario: **~60 min serial → <60 seconds** (60x+ speedup) with no accuracy compromise. Standard-mode pipeline untouched.
   • Regression tests: 14 new unit tests in `tests/test_ai_first_clustering.py` cover canonicalization, amount bucketing, cluster grouping, high/low-confidence propagation, and unclusterable-solo handling.

- **Plaid Backfill Poller — Durable + Semaphore-Safe (2026-08-26)** — Replaced the initial (buggy) `plaid_delayed_backfill_sync` job-queue task with a two-part scheduler:
   • `schedule_plaid_backfill_poll(company_id, item_id, attempt)` persists `next_backfill_poll_at` + `next_backfill_poll_attempt` on the `plaid_items` doc, then spawns a fire-and-forget `asyncio.create_task` timer. Sleeps happen OUTSIDE the job-queue semaphore — critical at scale, because holding one of the 20 per-pod slots while sleeping 30 min would cause priority inversion at 1,000+ concurrent onboardings.
   • `reconcile_pending_backfill_polls()` scans the plaid_items collection at backend startup and re-arms every pending timer with the correct remaining delay. Sparse index on `next_backfill_poll_at` keeps the scan O(pending) even with 1M items.
   • The actual sync work still routes through `enqueue_job("plaid_manual_sync", …)` so the sync semaphore protects real work only.
   • Chain: +30s / +2m / +5m / +15m / +30m. Stops early on `historical_update_received: True` or when the requested `import_start_date` floor is reached. Final attempt stamps `webhook_code="HISTORICAL_UPDATE"` so opening-balance JEs still land when Plaid never fires its own webhook.
   • Regression tests in `tests/test_plaid_delayed_backfill.py` (4 tests: persist+timer, past-max no-op, reconciler re-arm, malformed date resilience).




- **Multi-Account Combined Statement Fan-Out (2026-08-25)** — A single Veryfi bank-statement that lists MULTIPLE accounts on one PDF (Wells Fargo Combined, Amex Blue + Gold, Chase Checking + Savings, etc.) is now handled correctly.
   • Detection: When `veryfi_data.accounts[]` has 2+ entries and no explicit `account_id` was pinned, `_process_veryfi_result` fans out to `_fan_out_multi_account` instead of flattening.
   • Per-account child rows: The current import becomes a PARENT (`is_multi_account=True`, `is_multi=True`) and each account gets its own child `statement_imports` row with a synthetic single-account sub-doc via `veryfi_service.iter_statement_accounts`. Each child runs the full existing pipeline unchanged — resolver, OCR guards (Layers 1–4), Layer 3 reconcile against its OWN per-account totals, OBE JE, auto-recon, categorization.
   • New helper: `veryfi_service.iter_statement_accounts(veryfi_doc)` returns `[{account_ref, lines}]` — one entry per account, or a single entry for legacy/single-account shapes. Regression tests in `tests/test_multi_account_statement.py`.
   • UI: Parent renders with a `multi · N` badge and children indent with `↳` — same visual treatment as the splitter parent/child rows.

- **Veryfi Multi-Statement Splitter (2026-08-25)** — Support for combined-PDF or `.zip` bank-statement uploads via Veryfi's `/api/v8/partner/bank-statements-set` async endpoint.
   • UI: `☐ This file contains multiple separate statements` checkbox in the pre-check modal (default OFF, auto-ON for `.zip`). Copy explicitly warns NOT to use for combined-account statements — those are auto-handled by the new fan-out.
   • Backend: `statements.upload_statement_multi()` posts to splitter, creates a parent `statement_imports` row with `status='splitting'`. Returns immediately.
   • Webhook: `POST /api/webhooks/veryfi/bank-statement-set` (public, HMAC-SHA256 verified via `VERYFI_CLIENT_SECRET`) fetches each child `document_id` and runs `_process_veryfi_result()` per child. Children of the splitter can themselves be multi-account statements and fan out again automatically.
   • Refactor: Extracted post-Veryfi logic in `upload_statement()` into `_process_veryfi_result()` so sync + splitter + multi-account paths all share the same pipeline.


- Phase 2 hybrid QBO migration with dual-basis GL pull (`qbo_gl_lines`).
- Report engine reads directly from GL for QBO companies; legacy `_signed_balances` for native/Plaid.
- Superadmin GL Lab (`/admin/qbo-gl-lab`) for parity spot-checks.
- Date-range preset picker across P&L, BS, Cash Flow, GL.
- QBO Reconciliation panel with CSV export + auto-refresh.
- Full backfill endpoint (`POST /api/admin/qbo/full-backfill`) with per-company parity output.
- **Preview DB cleanup (2026-08-23)** — purged 945 test companies + 248 orphan users + 22 test enterprises. Backups in `/app/backups/`.
- **Native document JE posting (2026-08-23)** — `posting_service.py` now auto-posts double-entry JEs on:
   invoice create/update/delete, bill create/update/delete, payment (in/out) create/update/delete,
   sales receipt create/update/delete, estimate → invoice conversion, and PO → bill conversion.
   Cascade helpers in `link_cascade.py` and `transactions.py::_reverse_and_delete_payment` also
   reverse the JE so orphan ledger legs can't survive a cascade delete. Reports engine gated on
   `posted: True` to prevent double-counting. Native Accrual Balance Sheet now balances
   (Assets = L + E) end-to-end. Includes:
   - Type-scoped `_resolve_account` (was accidentally matching "Sales Tax Payable" for revenue).
   - Cash-basis filter for `auto_accrual` JEs (invoice-at-issue only recognizes on accrual).
   - `auto_cash` tag on receipt JEs so cash-basis P&L still surfaces sales receipts.
   - QBO-sourced docs defensively skip local JE posting (they use the GL / synthesis path).
   - Backfill script `scripts/backfill_document_jes.py` re-postable across all doc types.
- **QBO Recon 5-column overlay (2026-08-23)** — Reconciliation panel on BS + P&L now
   renders `Line | Official QBO | Imported QBO | + Native | Our Report | Δ vs QBO`.
   Backend adds an `imported_only=true` query param on the report endpoints that returns
   the GL-only slice. Legend cards show 3 trust KPIs (migration parity / native
   adjustments / unattributed drift) + filter toggles (All rows / Migration drift only /
   Native adjustments only). CSV export mirrors all 5 columns. Verified on
   Test QBO 431 LLC: `Imported = Official` (green ✓ across every row), `+Native = +$50k`
   on A/R fully attributes the drift, `Unattributed = $0`.
- **QBO + Native additive overlay (2026-08-23)** — `_signed_balances` on QBO-connected
   companies now layers native activity (invoices/bills/JEs/txns created in Axiom
   post-migration) on top of `qbo_gl_lines`. `_superseded_by_gl_ids` prevents double
   counting for docs that have been mirrored back to QBO. `_open_ar_ap` filters
   `source='qbo'` when GL data exists. Every doc counts exactly once in exactly one lane.
- **Deposit guard + UF safety net (2026-08-23)** — `merchant_cache.categorize_with_cache`
   force-routes every positive-amount Plaid txn to `4999 Uncategorized Income` (with
   `needs_review=true`) unless a deterministic rule matches (Interest, Bank Fee reversal).
   The LLM is never asked to guess an income account. Belt-and-suspenders post-LLM override
   refuses any account whose `detail_type=money_in_transit` for a bank-feed txn. Historical
   sweep on Plaid Test LLC re-routed 8 mis-categorized DDA→DDA deposits to 4999; UF is no
   longer negative and BS now balances. Rules Miner also blocks learning rules that target
   `4999 / 6999 / money_in_transit` so bad historical patterns can't get codified.
- **Balance Sheet parent drill-down fix (2026-08-25)** — `reports.compute_account_detail`
   now walks `parent_account_id → id` breadth-first to include every descendant sub-account
   in the transaction query. Clicking a parent row on the BS ("Business Checking +1 sub")
   surfaces the roll-up transactions instead of showing an empty "No transactions" page.
   Sub-account clicks still stay scoped to their own id (no upward leak). Regression test
   at `tests/test_account_drilldown_parent_sub.py`.

## Explicitly Declined (do not resurface without user approval)
- **Multi-account fan-out for combined statements** (server-side PDF page-range splitter,
   "Option 2" from the Aug 25 discussion). User accepts that combined statements Veryfi
   can't split automatically will be handled by manual pre-upload PDF splitting.
- **False-RECONCILED status bug** (auto-recon marking as RECONCILED when |diff|>$0.01).
   Known behavior; not fixing.
- **Layer 6 OCR defense** (detect `account_numbers` plural mismatch and refuse auto-recon).
   Not building; workflow is manual PDF split when Veryfi conflates accounts.

## Prioritized Backlog

### P0 — In progress
- (none active)

### P0 — Awaits user action
- (none — Emeral Coast re-auth dropped 2026-08-23; parity gap accepted as-is on prod)

### P1
- QBO Piece 2: AI Merchant → Contact match (removes "?" placeholders)
- Recon panel label alias map (Advertising ↔ Advertising & Marketing)
- Enterprise WL self-service toggle
- PDF export on QBO Reconciliation panel
- Real-time inbound webhooks for QBO (Phase 4)
- `_signed_balances` rewrite to Mongo `$group` aggregation
- AI rule confidence sort on Rules page (sort miner rules by `mined_confidence` DESC)
- **Runtime Rules Precedence** — wire `db.rules` into `merchant_cache.categorize_with_cache` as Step 0 (before static merchant_rules → cache → PFC → LLM), so user-created rules from the Rules page apply to incoming Plaid txns instead of only retroactively via `apply_to_existing`. ~20-line change + Mongo index on `(company_id, match_type, match_value)`. Discovered 2026-08-23 during rules-flow audit.

### P2
- Dead OAuth orphans cleanup (`qbo_connections` for deleted companies)
- Set-Password page design polish (lockup / accent color)
- Restaurant Vertical foundation (locations, multi-loc reports, feature flags)

### P3 / Backlog
- QBO Attachable file downloads
- Background auto-pull polling
- Native `.IIF` QBD import
- Transaction CSV import in Transactions page (paused)

## Preview Cleanup Scripts (2026-08-23)
- `/app/backend/scripts/purge_test_companies.py` — deletes non-protected companies + child rows across 52 collections
- `/app/backend/scripts/purge_orphan_users_enterprises.py` — initial orphan cleanup
- `/app/backend/scripts/purge_final_pass.py` — email-pattern-based user purge (STOP backend before running to avoid Firm Books auto-reseed loop from `enterprises.py:307`)

## Notes for Future Prod Cleanup
Before running any cleanup on prod, add to protected set:
- Enterprises: **CypherPro, Aquila Tax Services, ProactiveBooks**
- Partners: **Partner, ScarlettBooks**
- All companies inside them + their users
